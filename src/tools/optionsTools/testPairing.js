import stimuli from '../helpersTools/stimuli';

const testPairing = () => {
    stimuli("vibration", 230, defAT, "Incoming Vibration. You should receive a notification on your phone, followed by a vibration");
};

export default testPairing;